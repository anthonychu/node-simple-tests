const assert = require('assert');

describe('Some other tests', function() {
  it('1', function() {
    assert.equal(1, 1);
  });

  it('2', function() {
    assert.equal(1, 1);
  });

  it('3', function() {
    assert.equal(1, 1);
  });

  it('4', function() {
    assert.equal(1, 1);
  });

  it('5', function() {
    assert.equal(1, 1);
  });

  it('6', function() {
    assert.equal(1, 1);
  });

  it('7', function() {
    assert.equal(1, 1);
  });

  it('8', function() {
    assert.equal(1, 1);
  });

  it('9', function() {
    assert.equal(1, 1);
  });
});